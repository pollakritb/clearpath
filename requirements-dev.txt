# ClearPath backend - local development + tests
-r requirements.txt
uvicorn[standard]>=0.30
pytest>=8.0
pytest-cov>=6.0
pip-audit>=2.9
ruff>=0.9,<1.0
# Kriging upgrade: local analysis only; excluded from the deployment bundle.
scipy>=1.13
pykrige>=1.7
# Evaluation metrics and report figures (scripts/evaluate_interpolation.py).
matplotlib>=3.8
xgboost>=3.0,<4.0
