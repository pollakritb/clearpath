# ClearPath backend - production / deploy (Vercel)
# Keep the deployment set small enough for the 250 MB function limit.
fastapi>=0.115,<1.0
httpx>=0.27
numpy>=2.0
supabase>=2.7
pydantic>=2.8
pydantic-settings>=2.4
python-dotenv>=1.0
python-multipart>=0.0.20
Pillow>=12.3,<13.0
pywebpush>=2.0,<3.0
truststore>=0.9  # Use the OS trust store for Air4Thai HTTPS.
